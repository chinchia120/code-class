#include <iostream>
#include <stdio.h>
#include <string.h>
using namespace std;

int main()
{
    int n;

    while (scanf("%d", &n) != EOF)
    {
        int arr[n];

        if (n == 0)
        {
            break;
        }

        for (int i = 0; i < n; i++)
        {
            scanf("%d", &arr[i]);
        }

        for (int i = 0; i < n; i++)
        {
            int x1, x2;
        }
    }

    return 0;
}