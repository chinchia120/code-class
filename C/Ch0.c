#include <stdio.h>

int main()
{
    float x, z;
    int y;

    printf("enter x, y, z, ");
    
    scanf("%f %f", &x, &z);
    printf("%f,%f", x, z);
    
    //printf("x= %f, y= %d, z= %f\n", x, y, z);

    return 0;
}
